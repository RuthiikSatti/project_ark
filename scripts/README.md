# Scripts

This directory is reserved for scripts that automate repeatable ARK administration and maintenance tasks.

Examples of future automation include:

- service health checks
- backup verification
- storage checks
- maintenance tasks
- deployment helpers
- system monitoring

Scripts should be safe, documented, and tested before being used against the live ARK environment.

Credentials and secrets must never be hard-coded into scripts or committed to the public repository.
